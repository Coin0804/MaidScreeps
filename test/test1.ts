
export const testFn = function (num1: number, num2: number): number {
    Infinity
    let o = {}
    o["the_stealth_warrior"] = 0
    Object.keys(JSON.parse(JSON.stringify(o)))[0];
    "".split(/[-_]/).map((s) =>[s[0].toUpperCase(),s.substring(1)].join('')).join('');
    
    return num1 + num2
} 
